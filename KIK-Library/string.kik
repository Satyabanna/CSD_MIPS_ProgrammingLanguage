// KIK Standard Library: String Utilities
// Filename: string.kik

/*
 * Converts a string of digits into an integer.
 * This is a pure KIK function.
 */
int str_to_int(str s) {
    int num = 0;
    
    // Use the KIK 'for' loop syntax
    for int i = 0; i < s.length();: i = i + 1: {
        num = num * 10 + (s[i] - '0');
    }
    return num;
}